# Sequence of Prime Numbers of Arbitrary Length

Write a function that outputs an array containing the first `n`
prime numbers.

The function signature is:

```typescript
primesOfLength(length: number): number[]
```
