/**
 * List interface.
 *
 * @typedef List
 * @property {() => number} length Number of elements in the list.
 * @property {any} first Get the data located at the start of the list.
 * @property {(data: any) => void} insertStart Insert data at the start of
 * the list.
 * @property {(index: number) => any} at Get the data located at the specified
 * index.
 */
